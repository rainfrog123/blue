# Host: ali (Aliyun Singapore)

Site secrets only. Shared stacks: `cloud/common/stacks/`.

```bash
bash cloud/common/stacks/up-all.sh ali
```

Stacks: hysteria, ss-rust, xray-trojan, cloudflared.
Provider CLI: `cloud/providers/alibaba/`.
