# ali-jp xray-trojan

Origin for Cloudflare tunnel hostname **`ajp.hyas.space`**.

```bash
bash cloud/common/stacks/xray-trojan/up.sh ali-jp
```

Site: `site.json`. Defaults: `common/stacks/xray-trojan/defaults.json`.
