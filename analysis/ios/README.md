# iOS RE workspace (local)

Lab artifacts for the vault’s **iPhone 6s Plus** (15.7.1 / Dopamine-RootHide).

| Path | Use |
| --- | --- |
| `kliq/` | Kliq IPA from `ipatool` on `digi_sg` (encrypted Store package) |
| `ipatool/` | Optional scratch for other Store downloads |

`*.ipa` / `*.tipa` are gitignored — keep secrets/binaries local.

Vault notes: Obsidian `Tech/RE/ipatool.md`, `Bypass MinimumOSVersion (6s Plus).md`, `TrollStore.md`.
