# Azure — acme + auth only (listen/masquerade/bandwidth from common defaults)
# ca: zerossl — LE rate-limited for this hostname until ~2026-07-24
# dir: persist certs on the host bind-mount (survive container recreate)
acme:
  domains:
    - hyaz.hyas.site
  email: admin@hyas.site
  ca: zerossl
  dir: /etc/hysteria/acme

auth:
  type: password
  password: NzPY5nTKThLxUb1MOJTuu6B0
