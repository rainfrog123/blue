# Host: ali (Aliyun Singapore)

Site secrets only. Shared stacks: `infra/cloud/common/stacks/`.

```bash
bash infra/cloud/common/stacks/up-all.sh ali
```

Stacks: hysteria, ss-rust, xray-trojan, cloudflared.
Provider CLI: `infra/cloud/providers/alibaba/`.
